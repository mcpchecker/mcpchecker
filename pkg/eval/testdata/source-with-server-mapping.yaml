kind: Eval
config:
  sources:
    upstream:
      repo: github.com/org/repo
      ref: v1.0.0
      serverMapping:
        their-server: my-server
  taskSets:
    - source: upstream
      path: tasks/test.yaml
